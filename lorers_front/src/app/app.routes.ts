import { Routes } from '@angular/router';
import { CreateEventComponent } from './shared/components/create-event/create-event';

export const routes: Routes = [
  { path: '', component: CreateEventComponent },
];